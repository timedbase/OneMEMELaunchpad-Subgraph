{
  "name": "onememe-launchpad-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for OneMEME Launchpad on BNB Smart Chain",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",

    "codegen:full": "graph codegen subgraph.full.yaml",
    "build:full": "graph build subgraph.full.yaml",
    "deploy:full": "graph deploy --config subgraph.full.yaml",

    "create-local": "graph create --node http://localhost:8020/ onememe/launchpad",
    "remove-local": "graph remove --node http://localhost:8020/ onememe/launchpad",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 onememe/launchpad",
    "deploy-local:full": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 --config subgraph.full.yaml onememe/launchpad"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.35.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.81.0",
    "matchstick-as": "^0.6.0"
  }
}
